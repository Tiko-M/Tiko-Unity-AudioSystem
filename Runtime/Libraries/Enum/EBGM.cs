namespace Tiko.AudioSystem
{
    public enum EBGM
    {
        // Examples — adjust to your project; keep numbers stable and append-only
        Menu = 0,
        Stage1 = 1,
        Boss = 2,
    }
}
