namespace Tiko.AudioSystem
{
    public enum ESFX
    {
        // ---- examples ----
        Click = 0,
        UIHover = 1,
        Explosion = 2,
        // Append new ones below; do not reorder existing values.
    }
}
